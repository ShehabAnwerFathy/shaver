/// Helper class that contain all application images and icons.
abstract class AppImages {
  /// Private const constructor to prevent getting an instance from class
  const AppImages._();

  /// Images folder path
  static const _imagesFolderPath = 'assets/images';

  /// Shaver image
  static const shaver = '$_imagesFolderPath/shaver.png';
}
